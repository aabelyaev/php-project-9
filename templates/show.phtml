<!DOCTYPE html>
<html lang="ru" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Анализатор страниц</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        .table-responsive {
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
        }
        .badge-status {
            font-size: 0.85rem;
            padding: 0.35em 0.65em;
        }
        .check-btn {
            transition: all 0.2s ease;
        }
        .check-btn:hover {
            transform: translateY(-2px);
        }
    </style>
</head>

<body class="d-flex flex-column min-vh-100">
<header class="sticky-top">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">
                <i class="bi bi-speedometer2 me-2"></i>SEO Analyzer
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/"><i class="bi bi-house-door me-1"></i> Главная</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/urls"><i class="bi bi-collection me-1"></i> Сайты</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>

<main class="flex-grow-1 py-4">
    <div class="container">
        <!-- Flash сообщения -->
        <?php if (!empty($flash['success'])): ?>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="bi bi-check-circle-fill me-2"></i>
                <?= htmlspecialchars($flash['success'][0]) ?>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        <?php endif; ?>

        <?php if (!empty($flash['error'])): ?>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>
                <?= htmlspecialchars($flash['error'][0]) ?>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        <?php endif; ?>

        <!-- Заголовок и основная информация -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="h2 mb-0">
                <i class="bi bi-link-45deg text-primary me-2"></i>
                <?= htmlspecialchars($url->getName()) ?>
            </h1>
            <form method="post" action="/urls/<?= $url->getId() ?>/checks">
                <button type="submit" class="btn btn-primary check-btn">
                    <i class="bi bi-arrow-repeat me-2"></i>Запустить проверку
                </button>
            </form>
        </div>

        <!-- Карточка с основной информацией -->
        <div class="card shadow-sm mb-5">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-4">
                        <h5 class="card-title text-muted">ID сайта</h5>
                        <p class="card-text fs-4"><?= $url->getId() ?></p>
                    </div>
                    <div class="col-md-4">
                        <h5 class="card-title text-muted">Дата создания</h5>
                        <p class="card-text fs-4"><?= htmlspecialchars($url->getCreatedAt()) ?></p>
                    </div>
                    <div class="col-md-4">
                        <h5 class="card-title text-muted">Статус</h5>
                        <p class="card-text">
                            <span class="badge bg-primary badge-status">Активен</span>
                        </p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Таблица проверок -->
        <h2 class="h4 mb-3">
            <i class="bi bi-clipboard2-check text-primary me-2"></i>
            История проверок
        </h2>

        <?php if (!empty($checks)): ?>
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead class="table-light">
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Статус</th>
                        <th scope="col">Заголовок</th>
                        <th scope="col">H1</th>
                        <th scope="col">Описание</th>
                        <th scope="col">Дата</th>
                    </tr>
                    </thead>
                    <tbody>
                    <?php foreach ($checks as $check): ?>
                        <tr>
                            <td><?= $check->getId() ?></td>
                            <td>
                    <span class="badge bg-<?= $check->getStatusCode() >= 400 ? 'danger' : 'success' ?>">
                      <?= $check->getStatusCode() ?? 'N/A' ?>
                    </span>
                            </td>
                            <td class="text-truncate" style="max-width: 200px;" title="<?= htmlspecialchars($check->getTitle() ?? '') ?>">
                                <?= htmlspecialchars($check->getTitle() ?? '') ?>
                            </td>
                            <td class="text-truncate" style="max-width: 200px;" title="<?= htmlspecialchars($check->getH1() ?? '') ?>">
                                <?= htmlspecialchars($check->getH1() ?? '') ?>
                            </td>
                            <td class="text-truncate" style="max-width: 200px;" title="<?= htmlspecialchars($check->getDescription() ?? '') ?>">
                                <?= htmlspecialchars($check->getDescription() ?? '') ?>
                            </td>
                            <td><?= $check->getCreatedAt() ?></td>
                        </tr>
                    <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        <?php else: ?>
            <div class="alert alert-info">
                <i class="bi bi-info-circle-fill me-2"></i>
                Проверки для этого сайта еще не проводились
            </div>
        <?php endif; ?>
    </div>
</main>

<footer class="bg-dark text-white py-4 mt-auto">
    <div class="container text-center">
        <div class="d-flex justify-content-center gap-4 mb-3">
            <a href="#" class="text-white"><i class="bi bi-github fs-5"></i></a>
            <a href="#" class="text-white"><i class="bi bi-telegram fs-5"></i></a>
        </div>
        <p class="mb-0">© <?= date('Y') ?> SEO Analyzer | Разработано в <a href="https://hexlet.io" target="_blank" class="text-primary">Hexlet</a></p>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>